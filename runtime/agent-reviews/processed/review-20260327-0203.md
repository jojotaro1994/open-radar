# Supervisor Review: introduce-intelligence-operating-model

**Review ID:** review-20260327-0203
**Date:** 2026-03-27
**Reviewer:** claude-code-supervisor
**Cycle:** Round 3 — checking for new commits and regression

---

## Repo State Summary

Commit `447cae8` (feat: wire EvidenceRequest write path and fix tasks.md checkboxes) confirmed the Round 2 fix was applied correctly. `evidenceRequestStore.save(req)` is now called at `radar-cli.ts:1216` inside the `onApply` callback.

**Build + tests:** Pass cleanly (TypeScript compile + all invariant tests pass).

**Still unresolved from prior rounds:**
- Task 4 (pipeline reorder): unchanged, still the most critical structural gap
- Task 6 (RetrospectiveCase + LearningMemory write paths): stores instantiated, `buildRetrospectiveConfirm` and `buildLearningMemoryConfirm` implemented in `review-handler.ts`, but no CLI command wires them to `.save()`

**New concern:**
- 18 files uncommitted — includes both intelligence-operating-model files and cross-change syncs to `recenter-cli-radar-copilot` artifacts

---

## Findings (ordered by severity)

### HIGH

**f1 — RetrospectiveCase and LearningMemory write paths are not wired**

`/retrospective` and `/learning` are read-only list commands. `RetrospectiveCaseStore` and `LearningMemoryStore` are instantiated at `radar-cli.ts:891-892`, and `buildRetrospectiveConfirm` + `buildLearningMemoryConfirm` exist in `review-handler.ts`, but neither store's `.save()` is ever called. Task 6 CLI write flow is incomplete.

```
src/cli/radar-cli.ts:891  new RetrospectiveCaseStore(DATA_DIR)
src/cli/radar-cli.ts:892  new LearningMemoryStore(DATA_DIR)
src/cli/radar-cli.ts:1238 /retrospective → showRetrospectives() [read-only]
src/cli/radar-cli.ts:1246 /learning → showLearningMemory() [read-only]
```

**Recommendation:** Add `/retro submit <decisionObjectId> <reopenReason>` command that calls `buildRetrospectiveConfirm` and persists via `retrospectiveStore.save()`. Add `/learning add <retroId> <memoryType> <statement>` command similarly.

---

**f2 — Pipeline ordering unchanged (Task 4 still open)**

`run-pipeline.ts` still creates `MeetingRecord` at triage (line 436) before `DecisionObject` (line 496). `ba-meeting-room.ts` still takes `signalId`, not `DecisionObject`. This was deferred in Round 2 and remains the most important structural change needed.

**Recommendation:** Begin pipeline reordering — create `DecisionObject` first (Step 7b), then run a second meeting evaluation pass. Keep legacy `MeetingRecord` for backward compatibility.

---

### MEDIUM

**f3 — 18 uncommitted files, including cross-change artifact modifications**

`openspec/changes/recenter-cli-radar-copilot/proposal.md` and `tasks.md` are in the uncommitted diff alongside intelligence-operating-model work (`knowledge-pack`, `meeting-charter`, `search-context` updates). If the `recenter-cli` artifact changes are intentional cross-change syncs, they should be committed separately to maintain change boundary clarity.

**Recommendation:** Split commits by change — commit intelligence-operating-model files separately from recenter-cli-radar-copilot artifact modifications.

---

## Recommended Next Focus

1. **Wire RetrospectiveCase write CLI** — add `/retro submit <decisionObjectId> <reopenReason>` command; call `buildRetrospectiveConfirm` and persist via `retrospectiveStore.save()`
2. **Wire LearningMemory write CLI** — add `/learning add <retroId> <memoryType> <statement>`; call `buildLearningMemoryConfirm` and persist via `learningMemoryStore.save()`
3. **Begin Task 4 pipeline reordering** — move `DecisionObject` creation before `MeetingRecord`; add `DecisionObject`-centered meeting evaluation as second pass

---

## What to Verify Before Next Round

- [ ] `grep -r "retrospectiveCaseStore\.save\|learningMemoryStore\.save" src/` returns at least one match
- [ ] `npm run build` still passes cleanly
- [ ] `npm test` still passes cleanly
- [ ] Pipeline reordering work has begun (DecisionObject before MeetingRecord)
- [ ] Cross-change uncommitted files are either committed separately or confirmed as intentional syncs
