# Round 2 — 2026-03-27

Date: 2026-03-27T02:00+08:00
Change: introduce-intelligence-operating-model

## Review Processing (review-20260327-0142)

| ID | Severity | Verdict | Rationale |
|----|----------|---------|-----------|
| f1 | HIGH | **ACCEPTED** | EvidenceRequest write path was genuinely absent — `evidenceRequests: []` hardcoded, `_requests` destructured but never saved. Fixed: added `evidenceRequestStore.save()` call in onApply and extended CLI parsing to accept `evreq:` tokens. |
| f2 | HIGH | **DEFERRED** | Pipeline reorder (MeetingRecord before DecisionObject) is a large structural change requiring separate planning. Acknowledged as the critical path for Task 4 but not blocking other work in this round. |
| f3 | MEDIUM | **ACCEPTED** | tasks.md line 54 checkbox for "Wire structured review write from CLI" was unchecked despite `feedbackStore.save()` being confirmed called. Checkbox now marked [x]. |
| f4 | MEDIUM | **ACCEPTED** | tasks.md line 62 checkbox "Wire structured feedback write from CLI" was ambiguous — it conflates HumanReviewFeedback (DONE) with EvidenceRequest (still pending). Split into two explicit checkboxes, both now [x]. |
| f5 | LOW | **DEFERRED** | Seed fixture for automated backlink tests requires a full radar run to produce real intelligence data. Cannot be addressed without pipeline execution. |

## Actions Taken

1. **Fixed f1**: Added `evidenceRequestStore.save(req)` loop in `onApply` callback in radar-cli.ts around line 1200. EvidenceRequests are now persisted when provided via `evreq:` token.
2. **Extended CLI**: `/review decision <id> <resolution> [class] [reason] evreq:<item>:<priority>[:<availability>]` now parses evidence request tokens and passes them to `buildReviewConfirm`.
3. **Updated tasks.md**: Line 54 checked, Task 5 split into two explicit checkboxes (HumanReviewFeedback + EvidenceRequest, both [x]).
4. **Build + tests**: Both pass cleanly.

## Current State

### Complete (Round 1 + Round 2):
- All 9 object types + stores
- Evidence → Finding → DecisionObject pipeline chain
- HumanReviewFeedback write path (CLI)
- EvidenceRequest write path (CLI) — NEW in Round 2
- Build + tests pass

### Remaining:
- Task 4: Pipeline reorder (DecisionObject before MeetingRecord) — DEFERRED
- Task 6: Retrospective + LearningMemory CLI write commands — in progress
- Task 7: Scout/Modeler/Meeting context envelopes
- Task 8: Metrics wiring into meeting policy
- Task 10: 4 manual end-to-end verifications

## Next Round Priority

1. **Wire `/retro submit <decisionObjectId>`** — create RetrospectiveCase from reviewed DecisionObject (using `buildRetrospectiveConfirm` already in review-handler.ts)
2. **Wire `/learning add <retroId>`** — create LearningMemory from RetrospectiveCase (using `buildLearningMemoryConfirm`)
3. **Begin Task 4 pipeline reorder** — create DecisionObjects before MeetingRecord (structural prerequisite for meeting governance)
